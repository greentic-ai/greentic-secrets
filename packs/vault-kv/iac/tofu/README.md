# Vault KV setup

This template creates a KV v2 mount for Greentic secrets. If Vault is managed elsewhere, treat this as a reference.

Update `terraform.tfvars.json` with your values, then run:

- `tofu init`
- `tofu plan`
- `tofu apply`
