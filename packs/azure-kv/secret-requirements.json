[
  {
    "id": "azure_client_secret",
    "type": "string",
    "description": "Client secret for service principal auth (omit when using managed identity or device code).",
    "required": false,
    "env": "AZURE_CLIENT_SECRET",
    "redact": true
  },
  {
    "id": "audit_sink_credentials",
    "type": "string",
    "description": "Credentials or token for the configured audit sink (Splunk/GCP/Azure/http).",
    "required": true,
    "redact": true
  }
]
