[
  {
    "id": "gcp_service_account_json",
    "type": "string",
    "description": "Service account JSON for auth when not using workload identity.",
    "required": false,
    "env": "GCP_SERVICE_ACCOUNT_JSON",
    "redact": true
  },
  {
    "id": "audit_sink_credentials",
    "type": "string",
    "description": "Credentials or token for the configured audit sink (Splunk/GCP/Azure/http).",
    "required": true,
    "redact": true
  }
]
