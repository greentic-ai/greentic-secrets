# Global environment configuration example for local development and PR validation.

# --- Broker / services ---
BROKER__BIND_ADDRESS=0.0.0.0:8080
BROKER__NATS_URL=nats://localhost:4222
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# --- PR lane backends (AWS, Azure, Vault emulators) ---
SECRETS_BACKENDS=aws-sm:local,azure-kv:emulator,vault:local

# LocalStack credentials (default LocalStack values)
AWS_ENDPOINT_URL=http://127.0.0.1:4566
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_SESSION_TOKEN=test
GREENTIC_AWS_KMS_KEY_ID=arn:aws:kms:us-east-1:000000000000:key/local

# Azure Key Vault (replace with your Service Principal + vault URL)
AZURE_TENANT_ID=00000000-0000-0000-0000-000000000000
AZURE_CLIENT_ID=00000000-0000-0000-0000-000000000000
AZURE_CLIENT_SECRET=replace-me
# Emulator URL (update to your vault hostname when testing against Azure)
AZURE_KEYVAULT_URL=https://127.0.0.1:8080
# Scope used to request AAD tokens; override only when targeting sovereign clouds.
AZURE_KV_SCOPE=https://vault.azure.net/.default
# Skip TLS verification for the emulator's self-signed cert. Set to 0 for real vaults.
AZURE_KEYVAULT_INSECURE_SKIP_VERIFY=1
# Optional: uncomment to force a static bearer token (emulator debug only)
# AZURE_KEYVAULT_BEARER_TOKEN=emulator
GREENTIC_AZURE_KEY_NAME=greentic-wrap-key

# HashiCorp Vault (local dev server)
VAULT_ADDR=http://127.0.0.1:8200
VAULT_TOKEN=root
